:80 {
    encode zstd gzip

    @backend path /api/* /docs /docs/* /redoc /redoc/* /openapi.json
    handle @backend {
        reverse_proxy 127.0.0.1:8000
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
