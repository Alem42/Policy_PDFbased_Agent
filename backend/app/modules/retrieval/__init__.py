"""Reusable knowledge retrieval and deterministic evidence assessment.

Import concrete interfaces from ``contracts`` and the facade from ``service``.
The package intentionally has no eager imports because document enrichment
uses context formatting while the retrieval service itself depends on the
document module.
"""
